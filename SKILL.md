---
name: lengmeng-illustrations
description: 生成冷檬风格的中文正文配图。用于用户发文章/帖子/文案后要求"配图、做插图、文章配图、正文配图、冷檬配图、配图建议、shot list"等任务；固定 IP 为"冷檬"——一个金色双丸子头、穿黄色蕾丝连衣裙的水彩软萌 Q 版小女孩。默认工作流第一步必须先询问配图尺寸/画幅，用户答复后再消化文案并生成配图。
---

# 冷檬正文配图

## 核心定位

为中文文章、帖子、博客、公众号内容设计和生成配图。目标是把文章里的关键判断、流程、结构、状态或隐喻，变成一张柔和、治愈、有故事感、干净可读的水彩手绘解释图。

固定视觉 IP 是"冷檬"：一个金色双丸子头、穿黄色蕾丝连衣裙的 Q 版小女孩。冷檬必须参与画面的核心动作，不能只是站在旁边当贴纸或装饰。

风格校准参考图见 `assets/coldmeng-character-ref.jpg`，生成前可读取核对冷檬的外形与水彩质感。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：风格 DNA、配色、背景、禁忌。
- `references/lengmeng-ip.md`：冷檬的形象、性格、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、原创隐喻方法、物件与动作池。
- `references/prompt-template.md`：单张生图提示词模板（含尺寸变量）。
- `references/qa-checklist.md`：生成后检查和迭代规则。

## 工作流

### 1. 第一步：先问配图尺寸（强制）

用户发来文案后，**不要立刻出图**。先根据文案用途，向用户确认配图画幅尺寸。一次性给出清晰选项，让用户直接选或给自定义值：

- `16:9 横版`：文章头图、公众号封面、Banner、PPT 配图。
- `1:1 方图`：小红书主图、社媒方图、商品卡片。
- `3:4 竖版`：小红书竖图、公众号次图、竖版内容封面。
- `4:5 竖版`：Instagram、小红书竖版。
- `9:16 竖版`：手机全屏、短视频封面、Story。
- `自定义`：用户给具体比例或像素（例如"横版 1920×1080""竖版 1080×1440"）。

话术要短，例如：

> 开始配图前先确认一下画幅，选一个即可：
> 1. 16:9 横版（公众号头图/Banner）
> 2. 1:1 方图（小红书主图）
> 3. 3:4 竖版（小红书竖图）
> 4. 9:16 竖版（手机全屏/短视频封面）
> 5. 其他（告诉我比例或像素）

只有当用户明确说"不用问，就用 XX 尺寸"或"按你说的来"时，才可以跳过询问并自行选定尺寸。**用户未答复尺寸前，不要调用生图工具。**

### 2. 消化正文

用户答复尺寸后，再读用户给的正文、链接、Markdown 文件或截图。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择"认知锚点"：核心判断、两个断点、输入输出闭环、分流、前后对比、承接路径、常见坑、角色状态变化。

### 3. 先出配图策略（shot list）

如果用户只是让你"配图/分析怎么配图"，先给 shot list，不要直接批量生图。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- 冷檬在图里做什么
- 建议元素
- 建议中文标注词

默认 3-6 张。文章很短时 1-2 张；长文也不轻易超过 8 张。够用就好。用户确认或说"开始生成"后，再进入下一步。

### 4. 单张生成

用内置 `image_gen` 每张单独生成，**不要把多张图拼在一张里**。每张图只讲一个核心结构。

尺寸必须使用用户选定的画幅：在 `image_gen` 调用里把用户选择的比例换算成 `height`/`width`（例如 16:9 → 2048×1152，1:1 → 2048×2048，3:4 → 1536×2048，9:16 → 1152×2048，按工具说明的推荐值取整）。自定义尺寸按用户给的像素或比例换算，保持比例不变形。

提示词必须包含：

- 用户选定的画幅比例
- 干净奶油白/浅米白背景，柔和水彩手绘
- 冷檬作为核心动作主体（外形严格按 `references/lengmeng-ip.md`）
- 大量留白，一张图只讲一个核心结构
- 少量暖色调中文手写标注
- 禁止 PPT 信息图、商业矢量插画、幼稚表情包、复杂背景

### 5. 检查与迭代

生成后按 `references/qa-checklist.md` 检查。出现以下问题，优先重生成或局部编辑：

- 冷檬只是装饰、站在角落看
- 画风变成矢量平涂或纯线稿，没有水彩感
- 画面太满、太像 PPT/流程图
- 冷檬形象走样（发型、发色、服装不对）
- 中文标注太多、错字严重
- 左上角出现"流程图/系统架构图/常见坑"等类型标题
- 背景不是干净浅色，出现噪点、浓重阴影、复杂纹理

### 6. 保存交付

把最终图保存到项目主目录下的：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

## 输出口径

- 问尺寸时一次给全选项，别挤牙膏。
- 策略输出短而准：每张图放哪、讲什么、冷檬在干嘛。
- 生成后交付：生成了几张、每张用途、保存路径、哪几张最稳。
- 不要长篇讲风格理论，让图自己说话。
